// abstract class is a class that is declared with the abstract ekyword , abstract classes are not interfaces
// it can hvae constructors
// abstract method is declared and does not have implementation is known as an abstract class (abstract class may or may not have have an abstract method)
// and classes which have absteact method 
// advantages:
// reduce the complexity, increase reusability 
// abstract class dont have objects


